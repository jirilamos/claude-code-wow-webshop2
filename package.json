{
  "name": "wow-webshop",
  "version": "1.0.0",
  "description": "WOW Webshop Demo",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "dev:windows": "start /B npm run dev --prefix backend & npm run dev --prefix frontend"
  },
  "devDependencies": {}
}
